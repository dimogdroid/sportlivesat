<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="difed.soccersat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application android:allowBackup="true" android:label="@string/app_name"
        tools:replace="android:icon"
        android:icon="@drawable/icon" android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ListaEventos" />
        <activity android:name=".ListaCanales" />
        <activity android:name=".GmzActivity" />
        <activity android:name=".AcercadeActivity" />
        <activity android:name=".AyudaActivity"
                  android:theme="@style/Theme.Transparent"/>
        <activity android:name=".AyudaFiltroActivity"
            android:theme="@style/Theme.Transparent"/>
        <activity android:name=".AyudaZoomActivity"
            android:theme="@style/Theme.Transparent"/>


        <receiver android:name=".AlarmReceiver"></receiver>

    </application>

</manifest>
